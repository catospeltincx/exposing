<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="css/main.css" />
  </head>
    <body>
    <main>
    <header>
      <a href="/index.html">exposing</a>
    </header>
        <h1>{{ title }}</h1>
        <img src= "{{ project_image }}" alt="{{ title }}"></img>
        {{ content }}
        </main>
    </body>
</html>